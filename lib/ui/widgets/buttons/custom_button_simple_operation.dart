import 'package:flutter/material.dart';

class CustomButtonSimpleOperation extends StatelessWidget {
  const CustomButtonSimpleOperation({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
